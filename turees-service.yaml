apiVersion: v1
kind: Service
metadata:
  name: turees-web-svc
spec:
  type: NodePort
  selector:
    app: turees-web
  ports:
    - port: 3000 # internal cluster port
      targetPort: 3000 # container port
      nodePort: 30080 # port you can access from any node
